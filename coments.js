comments = {}
for (var comentario in obj){
    console.log(property + " = " + obj[property]);
  }

for (var sub_comment of comments) {
    //el.addEventListener('click', marcarPonto);
    
    sub_comment.id = "point-"+count;
    pontos_chakra[sub_comment.id] = { 'id': sub_comment.id, 'valor': valor };
}

comentarios =
{
    'user': 'usuario',
    'comentario': 'texto...',
    'comments':
    [
        'user': 'Texto',
    ]
};

{
    "cand": [
        {
          "posicao": "1",
          "id": "@novo_uuser",
          "coment": "Ficou muito lindo!"
        },
        {
            "posicao": "1",
            "id": "@velho_uuser",
            "coment": "Ficou muito lindo!"
          },
      ]
]
    
